# Kali Linux Docker Compose

## RUN

```shell
docker compose run -it --rm kali /bin/bash
```

## Script helper

```shell
./kali-shell.sh            # abre /bin/bash por defecto
./kali-shell.sh /bin/zsh   # o cualquier otro comando
```
